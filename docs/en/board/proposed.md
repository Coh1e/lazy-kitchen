# Proposed

> All current `status: proposed` dishes. Auto-generated from `data/dishes.yaml` by `bun run scripts/build-html.ts`.

Each badge shows current status. Click in for detail; vote or comment via Giscus at the bottom.
